# Alineado con el paréntesis que abre la función
foo = long_function_name(var_one, var_two,
                         var_three, var_four)

# Nunca mezcles tabulaciones y espacios.Cualquier código indentado con una mezcla de espacios y
# tabulaciones debe ser convertido a espacios exclusivamente

# Limita todas las líneas a un máximo de 79 caracteres.
# En el caso de largos bloques de texto (“docstrings” o
# comentarios), limitarlos a 72 caracteres es recomendado.

# Las importaciones deben estar en líneas separadas, por ejemplo:
# import os
# import sys

"""Los comentarios en línea deberían estar
separados por al menos dos espacios de la sentencia. Deberían
empezar con un # (numeral) seguido de un espacio.

"""

# Nombramiento

# Los nombres de clases deben utilizar la convención “CapWords”.

# Las funciones y metodos deben ser en minúscula, con las palabras separadas por un guión bajo.